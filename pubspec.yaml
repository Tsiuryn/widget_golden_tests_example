name: widget_golden_tests_example
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  mockito: ^5.4.2
  golden_toolkit: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  integration_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.1.7

flutter:
  uses-material-design: true

  assets:
    - assets/bird_calc/

